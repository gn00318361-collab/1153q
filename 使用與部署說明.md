# 青溪國小六年級3Q達人評選系統

候選資料為本次規格提供的32筆：AQ 12筆、MQ 9筆、EQ 11筆。保留原始推薦事蹟；本系統不自動替學生評分。

## 檔案用途

- `Index.html`：完整評審介面與候選資料，可直接開啟預覽。正式雲端儲存必須由Apps Script部署網址開啟。
- `Code.gs`：儲存、讀取、版本衝突檢查、截止判斷、管理端統計。
- `appsscript.json`：時區與權限設定。
- `TEST-RESULTS.txt`：本機測試摘要。測試用模擬Google服務，沒有寫入正式試算表。
- `template.html`、`data.json`、`build.mjs`、`test.mjs`：開發用模板、資料、建置與測試程式。一般部署不用上傳這些檔案。

## 部署前待填的兩項資料

目前評審名單依規格保留六位具名教師：詹世羽、游智媚、李嘉傑、陳玟婷、陳佩欣、李偉甄。張芯未列入評審。

「四處主任／行政委員」是多人群組稱呼，若共用一個選項會互相覆蓋。因此尚未加入可送分名單；請取得實際行政評審姓名後，每位加入一個獨立選項。是否有其他六年級任課教師，請以校內實際課務名單確認。

截止日期尚未提供，`deadline` 暫為 `null`。此時由管理員手動開放／停止收件；初始化不會自動開放。

使用文字編輯器開啟 `Index.html`，搜尋 `id="app-data"`，只修改此JSON區段中的設定：

```json
"judgesList": ["詹世羽 老師", "其他實際評審姓名"],
"deadline": null
```

有截止時間時，把 `null` 改成帶台灣時區的日期時間字串，格式為 `YYYY-MM-DDTHH:mm:ss+08:00`。不要使用尚未確認的日期。後端也讀取同一份設定，不需重複維護評審與候選清單。修改後需更新部署版本。

## 第一次部署

1. 用管理員的Google帳號建立一份專用Google試算表。評審不需取得此試算表的編輯權限。
2. 試算表選「擴充功能 → Apps Script」。把 `Code.gs` 的全部內容貼入程式碼檔案。
3. 新增HTML檔，名稱填 `Index`，貼入 `Index.html` 的全部內容。
4. 在Apps Script專案設定開啟顯示資訊清單，將 `appsscript.json` 貼入同名檔案；時區為Asia/Taipei。
5. 儲存，在編輯器執行 `setup_`，依Google畫面完成管理員授權。此步會綁定這份試算表，建立「提交狀態」「評分明細」「評審進度」「總表」，不清除既有分數。
6. 選「部署 → 新增部署作業 → 網頁應用程式」。執行身分選部署者；存取範圍依校內帳號與Workspace政策選擇。複製部署後 `/exec` 網址。
7. 回試算表重新整理，出現「3Q評選管理」選單。完成名單與截止設定後，點「開放收件」。也可在編輯器執行 `openCollection_`。
8. 先以測試用試算表與副本試跑：單類存入、另一裝置讀取、修改重送、停止收件。正式版本使用乾淨的正式試算表；不要把測試分數混進正式評選。
9. 正式評審使用部署網址。每人選自己的姓名，即可閱讀、輸入分數、按「儲存此類別」。

Google Workspace若限制校外存取或網頁部署，需依學校管理政策設定。這份交付目前是可部署原始碼，尚未在您的Google帳號部署，沒有正式評選網址。

## 評審操作

- 每類所有人都填入0～100分後才可儲存，0分是有效分數，小數亦可。
- 每次輸入即存於本機，依「活動＋評審姓名＋類別」分開；換類別不會清空。
- 「本機草稿」不等於雲端已儲存。看到「✓ 已儲存」才是雲端確認結果；修改後會改顯示未送出。
- 換裝置選姓名會讀取雲端。原裝置仍有草稿時，若雲端版本不同，先選「使用雲端版本」或「保留本機草稿」，再送出。
- 斷線／逾時不清草稿，可重試或重新同步。已寫入但回應遺失時，同一請求不重複計分。
- 本機預覽不會假裝雲端儲存成功；也不會把本機檔案上傳。
- 如瀏覽器禁止暫存，頁面會提示，並可匯出草稿JSON。此匯出供保留備份，不是正式提交。

本版依指定採「選姓名」操作，並未驗證選擇者是否為該評審；持有網址者可選擇其他姓名。請將網址限供受邀評審使用。若需要身分驗證，須另加登入或個人驗證機制。

## 管理端與計分規則

- 「提交狀態」是一位評審、一個類別一列的正式儲存紀錄，分數以整類JSON一次寫入，請勿手動修改。
- 「評分明細」維持規格的七欄：時間戳記、評審委員、類別、班級、學生姓名、給分、學生ID。每次提交後由最新紀錄重建，同人重送不會增加權重。
- 「評審進度」顯示每位評審各類已存／未存。
- 「總表」按AQ、MQ、EQ各別計算完整有效提交的等權算術平均；顯示有效評審數、名次與暫定前三名。尚未完成的類別不納入平均，不當作0分。
- 排序使用未四捨五入的數值，顯示至小數2位。未收到任何有效分數者不顯示名次。
- 第三名邊界同分時列「待決議」，不以輸入順序逕自剔除同分學生。校方需確認同分處理規則後決定人選。
- 「停止收件」及截止時間皆由伺服器判斷，不能靠改手機時鐘繞過。停止後可讀取既有分數。
- 管理員可按「更新明細與總表」重建衍生表。若分數已存、總表更新失敗，評審頁會明確告知，不要求重填。
- `setup_`、`refresh_`、開放／停止與統計函式尾端加底線，供管理員編輯器／試算表選單執行，評審端不可用 `google.script.run` 呼叫。

規格提供的QUERY可作輔助核對。正式使用內建總表即可，不需額外貼公式；內建總表另處理有效提交、評審人數與第三名同分。

## 更新與資料保存

每次改程式後：儲存 → 部署 → 管理部署作業 → 編輯 → 新增版本 → 部署。既有網址可延用；請評審重新整理。

同一活動開始收件後不要任意修改候選ID、評審姓名或 `eventId`。新增一屆評選時請使用新的試算表、新專案及新的 `eventId`，讓本機草稿與雲端資料分開。

目前的 `Index.html` 是完整可部署成品。開發者也可修改 `data.json` 和 `template.html`，在此資料夾執行 `node build.mjs` 重建；這會覆蓋 `Index.html`，所以不要只改成品又重新建置。`test.mjs` 的瀏覽器套件路徑使用本機Codex環境，移至其他電腦執行時需調整Playwright與Chrome路徑。

## 測試範圍與官方參考

本機測試涵蓋有效與非法分數、缺人、重複ID、名單偽造、版本衝突、重送、寫入失敗、截止與停止收件、等權平均、同分、手機版草稿／跨裝置同步等。Google真實授權、Workspace政策及Sheets服務需部署後再驗收。

- [Google Apps Script 網頁應用程式](https://developers.google.com/apps-script/guides/web)
- [前端非同步呼叫與私有函式](https://developers.google.com/apps-script/guides/html/communication)
- [LockService並行寫入鎖](https://developers.google.com/apps-script/reference/lock/lock-service)
